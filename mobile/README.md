# 手机远程客户端

中文 · 本版隐藏了远程入口，功能将在下一版本发布；以下说明保留备用。原生 App 还没做。扫码打开的是官方 `dsh web`：竖屏用覆盖层侧栏，横屏常驻工作区侧栏。

## Web

用系统相机扫桌面端侧栏 **手机按钮** 弹出的二维码。密钥在 `#offer=` 里，不是查询串。配对成功后加载本机官方页。

## Android

原生页以后再做。WebView 若要用，打开的也是官方 UI，不要再套一套独立聊天页。
